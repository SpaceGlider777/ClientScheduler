package util;

import model.User;

/**
 * Utility class for holding the logged-in user.
 */
public class CurrentUser {
    /**
     * The logged-in user.
     */
    public static User LoggedInUser = null;
}
